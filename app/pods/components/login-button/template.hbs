
{{log session.isAuthenticated}}
{{#if session.isAuthenticated}} 
  <button class="button-solid" {{action 'invalidateSession'}}>Logout</button>
{{else}}
  <button class="button-solid" {{action 'Authenticate'}}>Login</button>
{{/if }}