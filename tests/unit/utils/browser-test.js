import browser from 'hiring-front/utils/browser';
import { module, test } from 'qunit';

module('Unit | Utility | browser', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = browser();
    assert.ok(result);
  });
});
